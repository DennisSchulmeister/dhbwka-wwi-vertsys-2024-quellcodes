/**
 * Beispiel für eine fachliche Anwendungsfunktion. Ermittelt zu einem gegebenen
 * Namen eine nette Begrüßung.
 * 
 * @param {string} name Zu grüßende Person
 * @returns {string} Begrüßung
 */
export function getGreeting(name) {
    return `Hallo ${name}`;
}